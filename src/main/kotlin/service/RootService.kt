package service

 class RootService { //  val playerActionService=PlayerActionService(this)
    /**
     * The currently active game. Can be `null`, if no game has started yet.
     */
    // var currentGame : GameState = null
}